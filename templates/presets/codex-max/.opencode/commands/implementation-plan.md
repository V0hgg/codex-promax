---
description: Create or refine an implementation plan using the shared context cache and current code paths.
agent: plan
subtask: true
---

Read the most relevant notes in `.agent/context/` first.

Trace the current code paths before proposing changes, then produce a milestone-based plan with:

- concrete file targets
- validation commands
- key risks or assumptions
