# Commands Reference

## Build

Document the canonical build command and any prerequisites.

## Test

Record the fastest targeted test commands and the full verification command used before merge or release.

## Local Runtime

Capture the commands used to start, stop, and inspect the local runtime or harness.

Include `codex-promax prompt telemetry` and the observability smoke commands if this repository uses the codex-max harness.

## Release

Note the publish or deployment path, plus any automation or workflow that owns the final release step.

Update this file whenever the preferred commands change.
