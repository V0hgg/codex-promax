# Verification Rule

- Do not claim completion without running the narrowest relevant validation commands first.
- Prefer targeted checks before broader suites, but widen coverage when a change touches shared paths.
- If a required check cannot be run, state that explicitly and record the residual risk.
