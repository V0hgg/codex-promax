---
id: veloran.standards.validation
kind: standard
scope: project
appliesTo:
  - "**"
priority: 50
status: active
lastVerified: 2026-05-05
expires: when validation commands change
source: generated-by-veloran
---

# Validation Standard

Every implementation task should end with the narrowest meaningful verification, then broader checks when the changed surface requires it.

For this repository, replace this placeholder with the real commands confirmed during `init-harness`, such as type checks, unit tests, integration tests, build commands, local smoke checks, or package dry runs.

Record a fact with the verified command, date, and evidence once the real validation path is known.
